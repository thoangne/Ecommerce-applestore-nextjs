Run chatbot local 


1) tải ollamba 
https://ollama.com/library 

tải model "llama3.1:8b-instant"  => kêu chatgpt chỉ 

2) pull docker 

tạo file docker-compose.yml rồi paste code ở dưới vào 

version: "3.9"

services:
  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant_storage:/qdrant/storage

volumes:
  qdrant_storage:

chạy docker-compose kêu chatgpt chỉ 

3) chạy  file typescript qdrant-init.ts trước, sau đó chạy tiếp file indexProducts.ts 
